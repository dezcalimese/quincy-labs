export function PageIntro({label,title,accent,description}:{label:string,title:string,accent?:string,description:string}){return <section className="page-intro section-pad"><div className="intro-top"><a href="/">Quincy Labs</a><span>/ {label}</span></div><div className="intro-grid"><h1>{title}{accent&&<><br/><em>{accent}</em></>}</h1><p>{description}</p></div></section>}
export function SectionLabel({number,children}:{number:string,children:React.ReactNode}){return <div className="section-label"><span>{number}</span><span>{children}</span></div>}
export function ArticleBlocks({blocks}:{blocks:{tag:string;text:string}[]}){return <div className="reading-copy">{blocks.map((b,i)=>b.tag==='h2'?<h2 id={`section-${i}`} key={i}>{b.text}</h2>:b.tag==='h3'||b.tag==='h4'?<h3 key={i}>{b.text}</h3>:b.tag==='li'?<ul key={i}><li>{b.text.replace(/^•\s*/,'')}</li></ul>:<p key={i}>{b.text}</p>)}</div>}
