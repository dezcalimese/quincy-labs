import {formatDate} from '@/lib/sanity.utils';
export default function ArticleLayout({title,author,publishedAt,minutes,category,original,children}:{title:string;author:string;publishedAt:string;minutes:number;category:string;original?:string;children:React.ReactNode}){return <main id="main-content"><header className="article-header section-pad"><a className="back-link" href="/insights">← All insights</a><div className="eyebrow">{category}</div><h1>{title}</h1><div className="article-header-meta"><span>By {author}</span><time dateTime={publishedAt}>{formatDate(publishedAt)}</time><span>{minutes} min read</span>{original&&<a href={original}>View original ↗</a>}</div></header><div className="article-layout section-pad"><aside className="article-sidebar"><a href="/insights">Back to the notebook ←</a></aside><article className="article-surface"><div className="reading-copy">{children}</div>{original&&<div className="article-source">Originally published by {author}. <a href={original}>Read on Substack ↗</a></div>}</article></div></main>}
