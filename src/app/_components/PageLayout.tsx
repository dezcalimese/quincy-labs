import {PageIntro} from '../editorial';
export default function PageLayout({children,title,description,eyebrow}:{children:React.ReactNode;title?:string;description?:string;eyebrow?:string}) {return <main id="main-content" className="legacy-editorial">{title&&<PageIntro label={eyebrow||title} title={title} description={description||''}/>}<div className="section-pad legacy-content">{children}</div></main>}
